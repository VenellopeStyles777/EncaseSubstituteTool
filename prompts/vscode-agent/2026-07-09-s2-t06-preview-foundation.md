# 2026-07-09 - S2-T06 Raw/Text/Hex Preview Foundation Prompt

Use this prompt to hand S2-T06 to the Stage 2 VS Code implementation agent.

```text
Implement ticket S2-T06: Raw/Text/Hex Preview Foundation.

Before editing, read these files:
- prompts/vscode-agent/2026-07-09-stage-2-familiarization.md
- Goal.md
- progression.md
- tickets/stage-2/S2-T06-preview-foundation.md
- tickets/stage-2/README.md
- app/backend/api/directory_listing.py
- app/backend/api/README.md
- app/backend/forensic_core/filesystem_adapter.py
- app/backend/forensic_core/image_stream.py
- app/backend/forensic_core/README.md
- app/fixtures/README.md
- app/docs/environment-readiness.md
- functionality.md
- plan.md
- review.md
- workflow.md

Context:
- Stage 1 is complete.
- S2-T01 fixture/dependency strategy is complete.
- S2-T02 read-only image byte-stream abstraction is complete.
- S2-T03 volume discovery boundary is complete.
- S2-T04 filesystem adapter boundary is complete.
- S2-T05 backend directory listing/file metadata view is complete.
- Current focus is only S2-T06.
- S2-T06 should add a bounded preview foundation for small raw/text/hex content.
- The current stub filesystem entries are metadata-only; they do not expose real evidence byte offsets.
- Use a dependency-free stub/fixture preview source or tiny generated bytes for tests. Do not pretend that real filesystem extraction exists yet.
- This is still backend-first Stage 2 work, not UI or executable packaging.
- Do not add export/recovery, hashing, case-store persistence, background jobs, real filesystem parsing, or native dependency requirements.
- Do not commit real evidence, raw disk images, E01 files, or filesystem images.
- Treat all source/evidence paths as read-only.
- Preserve provenance in every preview result.
- Missing content, unsupported modes, invalid ranges, decoding issues, and truncation must be structured statuses/warnings, not raw tracebacks.
- Do not commit or push. Stop for review after this ticket.

Before implementing:
- Briefly summarize your understanding of S2-T06.
- List the files you expect to create or modify.
- If you see a scope conflict between the docs and this prompt, pause and explain it instead of broadening the ticket.

Your task:
- Add a small backend preview layer, preferably under app/backend/api/ if that fits the existing local command/API boundary.
- Define a JSON-friendly preview result shape with fields such as:
  - schema version;
  - status code/ok/message;
  - mode (`raw`, `text`, or `hex`);
  - source path;
  - volume id and offset/length where known;
  - file id, file path, file name, and entry type where known;
  - requested offset and requested length;
  - returned byte count;
  - source/content size when known;
  - truncation flag and warnings;
  - read-only assertion;
  - adapter/source/provider name;
  - preview data appropriate to mode.
- Add a bounded preview callable, for example `preview_file(...)` or similarly named, that can preview known stub/fixture content.
- Add a stable stub preview source/provider for tests. It may map the S2-T04/S2-T05 stub file `/hello.txt` or `stub-file-hello` to known bytes such as `Hello, world!`.
- For `raw` mode, keep output JSON-serializable. A short base64 string or explicit byte-value list is acceptable; choose the simplest style that fits the existing codebase.
- For `text` mode, decode safely with a documented encoding default such as UTF-8 and visible replacement/error behavior.
- For `hex` mode, produce deterministic lowercase hex output or a simple offset-aware hex shape. Keep it small and testable.
- Enforce a maximum preview length. If requested bytes exceed the limit or source length, return bounded data with structured truncation status/warning.
- Add structured status values for expected outcomes, for example:
  - `ok`;
  - `preview_truncated`;
  - `file_not_found`;
  - `path_not_file`;
  - `unsupported_preview_mode`;
  - `invalid_range`;
  - `content_unavailable`;
  - defensive `preview_error`.
- Do not mutate directory listing or filesystem adapter result objects in place.
- Do not wire preview into a real UI.
- Do not write output files.
- Do not compute hashes.
- Do not add required `pytsk3`, The Sleuth Kit, `pyewf`, or libewf dependencies.
- Update app/backend/api/README.md and app/backend/forensic_core/README.md as needed.
- Update functionality.md for S2-T06 status and keep Manual Test as `Untested`.
- Update plan.md if the Stage 2 note needs to advance from S2-T05 to S2-T06.
- Update progression.md with what changed, what was learned, blockers if any, and the next step.
- Update review.md with a handoff section or review notes if useful.
- Set `tickets/stage-2/S2-T06-preview-foundation.md` to `Review` when implementation is complete.
- Set S2-T06 to `Review` in `tickets/stage-2/README.md` when implementation is complete.
- Run `python -m pytest` and report the result.

Likely files to create or modify:
- app/backend/api/file_preview.py or a similarly named API module
- app/backend/api/__init__.py if exports are needed
- app/tests/test_file_preview.py or a similarly named test file
- app/backend/api/README.md
- app/backend/forensic_core/README.md
- app/fixtures/README.md if fixture/preview-source policy needs clarification
- functionality.md
- plan.md
- progression.md
- review.md
- tickets/stage-2/README.md
- tickets/stage-2/S2-T06-preview-foundation.md

Tests to add:
- text preview for known stub content;
- hex preview for known stub content;
- raw preview is JSON-serializable;
- preview offset and length are bounded;
- preview truncation is explicit when length exceeds the configured maximum or content size;
- missing file/path returns structured status;
- directory entry/path returns structured `path_not_file` or equivalent;
- unsupported preview mode returns structured status;
- invalid negative offset/length returns structured status;
- provenance/read-only fields are preserved;
- no real evidence, native dependency, private fixture, or network access is required.

Scope boundaries:
- Do not implement real filesystem byte extraction.
- Do not parse real EWF, partition, or filesystem data.
- Do not export/recover files.
- Do not compute hashes.
- Do not add UI.
- Do not add case-store persistence or audit events.
- Do not require pytsk3, The Sleuth Kit, pyewf, or libewf.
- Do not commit real evidence, raw disk images, E01 files, or filesystem images.

Deliverable:
- A dependency-safe backend raw/text/hex preview foundation that can preview known stub or tiny generated content and preserve provenance for later UI/export stages.

Final handoff:
- Summarize files changed.
- Summarize behavior added.
- Report the exact pytest command and result.
- State any limitations or deferred work.
- Confirm you did not begin S2-T07 or Stage 3.

Stop after S2-T06 and hand off for review. Do not commit, push, begin S2-T07, or begin Stage 3.
```
