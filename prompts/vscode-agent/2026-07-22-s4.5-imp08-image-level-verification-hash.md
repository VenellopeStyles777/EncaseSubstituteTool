# 2026-07-22 - S4.5-IMP08 Image-Level Verification Hash Prompt

```text
Implement ticket S4.5-IMP08: Image-Level Verification Hash Artifact.

Current truth: S4.5-IMP01 through S4.5-IMP07 are reviewed and done, but user hands-on testing exposed a gap: the demo does not yet generate an independent hash over the full logical image. Stage 5 remains blocked. Do not start S5-T01 or any S5-T02+ work.

Read:
- tickets/stage-4.5/S4.5-IMP08-image-level-verification-hash.md
- app/backend/api/first_testing.py
- app/backend/forensic_core/image_stream.py
- app/backend/forensic_core/ewf_reader.py
- app/docs/manual-testing/stage-4.5-command-line-testing-guide.md
- tickets/stage-4.5/README.md
- tickets/stage-5/README.md
- review.md
- functionality.md

Implement an explicit `--hash-image` first-testing option that computes at least SHA-256 over the full EWF logical image stream in chunks and writes `outputs/image-hash.json`.

Do not treat stored EWF hash metadata, segment-container file hashes, selected-file hashes, or stub bytes as the independent full-image verification hash.

Required:
- default first-testing run keeps image hash `not_run`;
- `--hash-image` computes over the logical image stream;
- artifact includes status, algorithm, hexdigest when completed, bytes hashed, logical media size, byte-count match, provenance, warnings, read-only assertion, and source-modified assertion;
- manifest, command summary, unsupported sections, docs, and tests are updated;
- missing dependency/stream states are structured statuses.

Run:
- `.\.python312-embed\python.exe -m pytest app\tests\test_image_stream.py app\tests\test_first_testing_command.py`
- `.\.python312-embed\python.exe -m pytest`

Attempt the documented real-image hash command only if it can complete honestly. The local logical image is large. Do not fake completion. If it cannot complete inside the work session, mark the ticket Blocked or clearly in Review with the exact limitation, progress/throughput if available, and the command the user/reviewer should run. Stage 5 remains blocked either way.

When complete, mark S4.5-IMP08 as Review, keep S4.5-IMP09 and S4.5-IMP10 pending, and stop.
```
