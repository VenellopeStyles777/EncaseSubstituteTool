# 2026-07-17 - S4.5-IMP02A Metadata Warning Semantics Prompt

```text
Implement ticket S4.5-IMP02A: Metadata Warning Semantics Cleanup.

This is a narrow review-finding correction after S4.5-IMP02. Do not start S4.5-IMP03. Do not implement EWF-backed streams, partition parsing, filesystem parsing, E01-backed file content providers, file-list export, static HTML, search, timeline, UI, reports, deleted recovery, carving, packaging, or native dependency installation.

Before editing, read:
- tickets/stage-4.5/S4.5-IMP02A-metadata-warning-semantics.md
- tickets/stage-4.5/S4.5-IMP02-real-ewf-metadata-verification.md
- app/backend/forensic_core/ewf_reader.py
- app/tests/test_ewf_reader_adapter.py
- app/tests/test_intake_command.py
- app/tests/test_first_testing_command.py
- review.md
- log/documentation.md

Review finding:
- `_extract_metadata()` can currently emit `metadata_partial` solely because stored EWF hashes were read and `stored_hash_not_verified` was emitted.
- That is too broad. `metadata_partial` should mean an actual metadata field was missing/unavailable/failed, not merely that hash metadata was conservatively labeled as non-verification.

Implement:
- Adjust metadata warning construction so `metadata_partial` is emitted only for true metadata incompleteness/failures.
- Preserve `stored_hash_not_verified` as its own warning when stored hashes are read.
- Preserve missing-`pyewf` behavior and verification statuses.
- Preserve first-testing artifact shapes.
- Keep stored hash metadata separate from verification success.

Tests:
- Add/adjust dependency-free fake-`pyewf` tests proving complete metadata + stored hash does not emit `metadata_partial`.
- Keep/confirm a partial metadata test still emits `metadata_partial`.
- Keep/confirm stored hash metadata does not become verification success.

Run:

python -m pytest app\tests\test_ewf_reader_adapter.py app\tests\test_intake_command.py app\tests\test_first_testing_command.py
python -m pytest

Documentation:
- Mark S4.5-IMP02A as `Review` when implemented.
- Update Stage 4.5 indexes/status docs, progression, review, log, and prompt README.
- Keep S4.5-IMP03 through S4.5-IMP07 incomplete.
- Keep Stage 5 search/timeline blocked/deferred and S5-T02 through S5-T16 Draft.

Final handoff:
- Say S4.5-IMP02A is ready for review.
- List files changed.
- Summarize the warning-semantics fix.
- Give exact focused and full test results.
- Confirm no real evidence committed, no native dependency install, no stream/filesystem/content/file-list/HTML/search/timeline/UI/report work, no commit, no push.
- Stop after S4.5-IMP02A.
```
