# 2026-07-23 - S4.5-IMP09A File-Visible Navigation Correction Prompt

```text
Implement ticket S4.5-IMP09A: File-Visible Navigation Correction.

S4.5-IMP09 is in Review, but reviewer validation found two correction items:
1. The real-image `--demo-list-first-directory` smoke produced a real parser-backed nested listing, but `file_count = 0`; it showed directories only.
2. A reviewer-run explicit deeper path produced regular files, so the implementation can reach files, but the default demo path still does not meet the user's "see actual files within the image" bar.
3. A reviewer-run explicit nested file path returned `path_not_found`; known file paths should return `path_not_directory`.

Stage 5 remains blocked. Do not start S4.5-IMP10, S5-T01, or any S5-T02+ work.

Read:
- tickets/stage-4.5/S4.5-IMP09A-file-visible-navigation-correction.md
- tickets/stage-4.5/S4.5-IMP09-nested-directory-navigation.md
- app/backend/api/first_testing.py
- app/backend/api/directory_listing.py
- app/backend/forensic_core/filesystem_adapter.py
- app/tests/test_first_testing_command.py
- app/tests/test_directory_listing.py
- app/tests/test_filesystem_adapter.py
- review.md
- functionality.md

Required:
- Keep S4.5-IMP09 parser-backed one-directory navigation.
- Improve `--demo-list-first-directory` so it can continue in a bounded way from root-directory candidates into their direct child directories, stopping when it finds a parser-backed listing with `file_count > 0`.
- Do not recursively crawl the volume; keep the probe limited to root candidates and their direct child directories.
- Record selected depth, file-visible status, root candidate attempts, child candidate attempts, and existing type counts in `directory-listing.json`, `navigation-readiness.json`, manifest, command summary, and static HTML summary.
- Fix explicit `--list-directory-path` on a known nested regular file so it returns `path_not_directory`, not `path_not_found`.
- Do not read file contents, auto-select files, export/preview/hash/signature selected files, build search/timeline, or add UI/report/deleted recovery/carving/packaging.
- Shared handoffs must report counts/status/type counts only, not real names or internal paths.

Run:
- `.\.python312-embed\python.exe -m pytest app\tests\test_filesystem_adapter.py app\tests\test_directory_listing.py app\tests\test_first_testing_command.py`
- `.\.python312-embed\python.exe -m pytest`

Run the corrected real-image smoke:

`.\.python312-embed\python.exe -m app.backend.api.first_testing --evidence-dir ".\ Test Image" --first-segment "C16242-1-RL1-E003.E01" --case ".test-artifacts\first-testing\nested-navigation-file-visible-real-image" --output ".test-artifacts\first-testing\nested-navigation-file-visible-real-image\outputs" --demo-list-first-directory --redact-paths`

The smoke must produce directory navigation `ok` / `real_parser_backed` with `entry_count > 0` and `file_count > 0`, or mark S4.5-IMP09A Blocked with the exact counts and limitation. Also verify the nested file-path status correction.

When complete, mark S4.5-IMP09A as Review, keep S4.5-IMP10 pending, keep Stage 5 blocked, and stop. Send the completion/blocker response back here for reviewer checkover.
```
