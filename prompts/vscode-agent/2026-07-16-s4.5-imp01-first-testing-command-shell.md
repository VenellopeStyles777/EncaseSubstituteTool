# 2026-07-16 - S4.5-IMP01 First-Testing Command Shell Prompt

```text
Implement ticket S4.5-IMP01: First-Testing Command Shell And Case Workspace.

This is the first Stage 4.5 implementation slice required before Stage 5 search/timeline can proceed. Build only the command shell, safe case workspace, intake persistence, run manifest, command summary, audit artifact, and unsupported-section output.

Do not implement real EWF metadata, real EWF verification, EWF-backed streams, partition/filesystem parsing, E01-backed file content providers, file-list export, static HTML, search, timeline, UI, reports, deleted recovery, carving, packaging, or required native dependencies.

Before editing, read:
- prompts/vscode-agent/2026-07-15-stage-4.5-first-testing-familiarization.md
- tickets/stage-4.5/S4.5-IMP01-first-testing-command-shell.md
- tickets/stage-4.5/README.md
- tickets/stage-4.5/S4.5-T01-user-e01-handling-plan.md
- tickets/stage-4.5/S4.5-T02-manual-e01-intake-demo-plan.md
- tickets/stage-4.5/S4.5-T07-workflow-guardrail-review-optimization.md
- tickets/stage-5/S5-T01-readiness-and-stage-4.5-completion-gate.md
- tickets/stage-5/S5-T01A-stage-4.5-gate-language-hardening.md
- app/backend/api/intake.py
- app/backend/case_store/schema.py
- app/backend/api/README.md
- app/docs/manual-testing/stage-4.5-first-testing.md
- functionality.md
- progression.md
- review.md
- log/documentation.md

Implement:
- Add `app/backend/api/first_testing.py`.
- Expose the useful callable(s) from `app/backend/api/__init__.py`.
- Add focused tests in `app/tests/test_first_testing_command.py`.
- Add `python -m app.backend.api.first_testing`.
- Support direct `.E01` input and `--evidence-dir` plus `--first-segment`.
- Require `--case`.
- Support optional `--output`, `--case-name`, `--case-description`, `--actor`, `--adapter pyewf|stub`, `--json-only`, and `--redact-paths`.
- Reject `.E02+` primary input, unsupported extensions, missing input, conflicting input forms, and unsafe evidence/case/output overlap.
- Create `<case>/case.db`, `<case>/run-manifest.json`, `<case>/command-summary.txt`, `<output>/intake.json`, `<output>/case.json`, `<output>/audit.json`, and `<output>/unsupported-sections.json`.
- Reuse `run_e01_intake()`, `connect()`, `initialize_schema()`, `insert_case()`, `insert_evidence_source()`, `insert_audit_event()`, and `list_audit_events()`.
- Insert audit events for case creation, evidence intake completion, artifact writes, and run completion.
- Use run schema `stage4_5.first_testing_run.v1`.
- Treat successful orchestration with unavailable real parser features as `ok_with_unsupported_sections`.
- Write unsupported-section rows for S4.5-IMP02 through S4.5-IMP06-owned future features.
- Keep local JSON unredacted, but redact evidence root in console/summary text when `--redact-paths` is passed.

Tests must use only tiny dummy filenames under `.test-artifacts`; do not add real E01 fixtures.

Required tests:
- direct `.E01` plus `--adapter stub` creates DB/artifacts/evidence/audit rows;
- `--evidence-dir` plus `--first-segment` resolves the selected `.E01`;
- default pyewf dependency-unavailable path is honest and still writes unsupported output;
- `.E02` primary input is rejected before artifact writes;
- unsafe evidence/case/output overlap is rejected before artifact writes;
- `--json-only` prints parseable JSON;
- `--redact-paths` redacts console/summary but preserves local JSON source paths;
- no file-list/export/report/static HTML artifacts are created by S4.5-IMP01.

Documentation:
- Mark S4.5-IMP01 as `Review` when complete.
- Update Stage 4.5 indexes/status docs.
- Update backend/API docs and manual-testing docs with the new command and its limits.
- Update `functionality.md`, `progression.md`, `review.md`, and `log/documentation.md`.
- Manual E01 status remains `Untested` unless the user confirms a reviewed real E01 run.
- Keep Stage 5 search/timeline blocked/deferred and S5-T02 through S5-T16 as Draft.

Run:

python -m pytest

Final handoff:
- Say S4.5-IMP01 is ready for review.
- List files changed.
- Summarize command behavior and artifact layout.
- Summarize tests added and exact pytest result.
- State what remains unsupported for S4.5-IMP02 through S4.5-IMP06.
- Confirm no real E01 fixtures, native dependencies, parser behavior, search/timeline, UI, reports, commit, or push.
- Stop after S4.5-IMP01 and hand off for review.
```
