# 2026-07-16 - S4.5-IMP07 Command-Line Testing Guide Prompt

```text
Implement ticket S4.5-IMP07: Command-Line Testing Guide And Evidence Workflow Instructions.

Current truth: S4.5-IMP01 through S4.5-IMP06 are reviewed and done. S4.5-IMP07 is the last Stage 4.5 implementation-runway ticket before S5-T01 can be rerun. Do not start S5-T01 or any S5-T02+ work.

This is a documentation/instruction ticket. Do not implement parser behavior, install dependencies, create real evidence fixtures, add search/timeline, build UI, or create reports beyond the guide.

Read:
- tickets/stage-4.5/S4.5-IMP07-command-line-testing-guide.md
- tickets/stage-4.5/README.md
- tickets/stage-4.5/S4.5-IMP06-final-guardrail-review-handoff.md
- app/docs/manual-testing/stage-4.5-first-testing.md
- app/docs/manual-testing/README.md
- app/backend/api/README.md
- app/backend/api/first_testing.py
- review.md
- functionality.md
- log/documentation.md

Create or update:
- app/docs/manual-testing/stage-4.5-command-line-testing-guide.md
- app/docs/manual-testing/README.md
- app/docs/manual-testing/stage-4.5-first-testing.md
- relevant Stage 4.5/Stage 5/status indexes

The guide must include:
- prerequisites;
- evidence safety and privacy rules;
- exact PowerShell commands from the repository root;
- clear use of `.\.python312-embed\python.exe` for real E01 parser-backed commands;
- local ` Test Image/` no-selection command example if the folder exists;
- direct `.E01`, `--evidence-dir`/`--first-segment`, `--json-only`, `--redact-paths`, `--adapter stub`, and selected-file template examples;
- pytest commands, including full portable-runtime pytest;
- expected artifacts and output locations;
- inspection steps for manifest, command summary, unsupported sections, metadata, verification, stream, volumes, filesystems, root listing, file-list JSON/CSV, static HTML, and selected-file not-run artifacts;
- expected statuses and troubleshooting;
- proof boundaries: what each command proves and does not prove;
- the minimum demo path: case workspace, E01 intake, segment discovery, metadata/verification status, EWF stream, volume discovery, root filesystem listing, file-list JSON/CSV, static HTML summary;
- explicit out-of-scope notes for nested traversal, broad crawl, arbitrary auto-selection/export, full-text E01 content, search/timeline, UI/report system, deleted recovery, carving, packaging;
- reviewer transcript template.

Run:

.\.python312-embed\python.exe -m pytest

If ` Test Image/` exists, run this documented real-image no-selection smoke with outputs under `.test-artifacts/first-testing/` and `--redact-paths`, then record only non-sensitive counts/statuses:

.\.python312-embed\python.exe -m app.backend.api.first_testing --evidence-dir ".\ Test Image" --first-segment "C16242-1-RL1-E003.E01" --case ".test-artifacts\first-testing\manual-guide-real-image" --output ".test-artifacts\first-testing\manual-guide-real-image\outputs" --redact-paths

Expected local no-selection shape from reviewed prior smokes: exit 0, `ok_with_unsupported_sections`, 53 segments, metadata `metadata_available`, verification `not_supported`, EWF stream `ok`, partition-table volume discovery `ok` with 5 volumes, filesystem `ok`, root listing `real_parser_backed` with 11 entries, file-list JSON/CSV `ok` with 11 entries, static HTML summary created, selected-file operations `not_run`, `source_modified: false`, and `read_only_asserted: true`.

Do not run selected-file extraction against the real image unless the user explicitly approves a safe root entry. Do not quote sensitive metadata values, evidence-internal names/paths, or file content.

Mark S4.5-IMP07 as `Review`, update docs, keep Stage 5 search/timeline blocked until S4.5-IMP01 through S4.5-IMP07 are completed and reviewed, and stop after S4.5-IMP07.
```
