# 2026-07-16 - S4.5-IMP04 E01 File Content Providers Prompt

```text
Implement ticket S4.5-IMP04: E01-Backed Selected-File Content Providers.

S4.5-IMP03 is reviewed and done. The project can now open the local E01 set, discover partition-table volumes, parse a filesystem root, and produce real parser-backed root entries. Work only S4.5-IMP04. Do not start S4.5-IMP05 or any Stage 5 ticket.

Goal:
- Add the first selected-file byte path from a real parser-backed FilesystemEntry to existing preview/export/hash/signature surfaces.
- Reuse existing `preview_file()`, `export_file()`, `hash_file_content()`, `detect_file_signature()`, and `evaluate_extension_mismatch()` behavior. Do not write replacement implementations.

Read first:
- `tickets/stage-4.5/S4.5-IMP04-e01-file-content-providers.md`
- `tickets/stage-4.5/S4.5-T05-e01-file-content-provider-plan.md`
- `tickets/stage-4.5/S4.5-IMP03-ewf-stream-partition-filesystem.md`
- `app/backend/forensic_core/image_stream.py`
- `app/backend/forensic_core/volume_discovery.py`
- `app/backend/forensic_core/filesystem_adapter.py`
- `app/backend/api/directory_listing.py`
- `app/backend/api/file_preview.py`
- `app/backend/api/file_export.py`
- `app/backend/forensic_core/content_analysis.py`
- `app/backend/api/first_testing.py`
- relevant tests for preview, export, analysis, filesystem adapter, and first-testing.

Required implementation:
- Add a shared selected-file content reader, or equivalent narrow module, that reads bytes from parser-backed E01 filesystem files through the EWF image stream and pytsk3.
- Add thin provider wrappers for:
  - `PreviewContentProvider`
  - `ExportContentProvider`
  - `AnalysisContentProvider`
- Use `source_kind=\"real_parser\"` only when bytes are proven to come from the parser-backed E01 path.
- Never fall back to stub provider bytes while claiming E01-backed content.
- Preserve evidence, volume, filesystem, file, parser/source, provider, and read-only provenance.
- Add a bounded first-testing size policy. Preview/signature can be bounded; hash/export must refuse files above the documented in-memory limit unless you implement reviewed streaming support.
- Represent directories, deleted entries, special files, sparse/unsupported files, unreadable files, large files, missing content, and provider exceptions as structured statuses/warnings.

First-testing command integration:
- Add explicit selected-file options, such as `--selected-file-id`, `--selected-file-path`, or another clear form.
- If no selected file is requested, write honest `not_run` selected-file artifacts and do not auto-select/export/hash arbitrary evidence files.
- If a selected file is requested, run preview/export/hash/signature using the new providers and existing surfaces.
- Allowed artifacts: `selected-file-preview.json`, `selected-file-analysis.json`, `selected-file-export.json`, optional `selected-file-content.json` or `selected-file-readiness.json`, and export output/manifest only for an explicitly selected safe file and explicit export destination.
- Do not create file-list JSON/CSV, static HTML, search/timeline indexes, UI/report artifacts, deleted-recovery/carving artifacts, or broad filesystem crawls.

Privacy:
- Do not quote sensitive real evidence filenames, paths, metadata values, root entry names, or file content in shared summaries.
- With `--redact-paths`, keep evidence-root paths redacted in console and command-summary text.

Tests:
- Default tests must remain dependency-free and real-evidence-free.
- Add focused fake parser-backed tests for reader success/failure, provider wrappers, first-testing selected-file artifacts, large-file refusal, directory/deleted/unreadable handling, and no stub fallback.

Run:

.\\.python312-embed\\python.exe -m pytest app\\tests\\test_file_preview.py app\\tests\\test_file_export.py app\\tests\\test_content_analysis_hashing.py app\\tests\\test_content_analysis_signatures.py app\\tests\\test_first_testing_command.py
.\\.python312-embed\\python.exe -m pytest

If you add a new focused test file, include it in the focused run.

Optional real E01 smoke:
- Only run after automated tests pass and only if ` Test Image/C16242-1-RL1-E003.E01` exists.
- Do not auto-select arbitrary evidence files.
- Select only a safe, regular, allocated file at or below the documented size limit.
- Do not quote the real filename/path, metadata values, root entry names, or file content.
- Keep outputs under `.test-artifacts/first-testing/...`.
- If no safe selected file is available from current root-listing scope, report that honestly as `not_run` or `blocked_by_selection`.

When finished:
- Mark S4.5-IMP04 as `Review`.
- Update docs/status files named in the ticket.
- Keep S4.5-IMP05 through S4.5-IMP07 incomplete.
- Keep Stage 5 search/timeline blocked/deferred.
- Confirm no real evidence was committed, no broad file-list/static HTML/search/timeline/UI/report/deleted-recovery/carving/packaging work was done, and no commit/push was performed.
- Stop after S4.5-IMP04 and hand results back for review.
```
