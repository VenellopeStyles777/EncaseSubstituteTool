# VS Code Agent Prompt - S4.5-IMP12 Image Hash Progress And Interrupt Status

You are the VS Code coding agent for the EnCase substitute project. Implement only S4.5-IMP12 and report back to the review agent when finished.

## Current Context

The user manually tested the Stage 4.5 demo on 2026-07-24 and stopped a `--hash-image` run because it appeared to run forever. The local logical image is about 1 TB, so full hashing can legitimately be long-running, but the demo needs accurate progress/loading feedback and an honest interrupted/non-success status if stopped early.

S4.5-IMP11 is already in review from your prior handoff, but the user explicitly said not to review it and to push the next ticket. Do not mark S4.5-IMP11 done. Do not start S5-T02 or any Stage 5 search/timeline work.

## Read First

- `tickets/stage-4.5/S4.5-IMP12-image-hash-progress-and-interrupt-status.md`
- `tickets/stage-4.5/S4.5-IMP11-demo-identity-and-navigation-labels.md`
- `tickets/stage-4.5/README.md`
- `tickets/stage-5/README.md`
- `Goal.md`
- `readme.md`
- `plan.md`
- `functionality.md`
- `progression.md`
- `review.md`
- `log/documentation.md`
- `app/backend/api/first_testing.py`
- `app/backend/forensic_core/image_stream.py`
- `app/tests/test_image_stream.py`
- `app/tests/test_first_testing_command.py`
- `app/docs/manual-testing/stage-4.5-command-line-testing-guide.md`
- `app/docs/manual-testing/stage-4.5-demo-showcase.md`

## Implement

Implement the ticket exactly:

- Add accurate progress for full logical-image hashing based on bytes hashed from the EWF logical image stream and logical media size.
- Include bytes hashed, total logical size when known, percent, elapsed time, throughput, ETA when possible, and final status.
- Prefer a useful terminal progress bar/status for the hands-on PowerShell demo.
- Keep `--json-only` stdout parseable as final JSON; progress must go to stderr and/or a progress artifact.
- Add or update `outputs/image-hash-progress.json` if that is the cleanest way to preserve progress state.
- Preserve the completed `outputs/image-hash.json` meaning: digest only when hashing completes successfully.
- On interruption, record a non-success status such as `interrupted`, include bytes hashed so far/logical size/elapsed/read-only/source-modified/warnings where available, and omit the digest.
- Preserve default no-hash behavior: `image-hash.json` remains `not_run`.
- Preserve redaction behavior.

Do not use stored EWF hash metadata, segment container file hashes, estimated file counts, or spinner-only UI as the proof of logical-image hash progress.

Do not add Stage 5 search/timeline, recursive crawl, broad enumeration, selected-file auto-analysis, UI/reporting, deleted recovery, carving, packaging, dependency installation, private evidence commits, commit, or push.

## Testing

Use dependency-free fake streams or small fixtures for automated tests. Do not require the local 1 TB real E01 hash to finish in tests.

Run:

```powershell
.\.python312-embed\python.exe -m pytest app\tests\test_image_stream.py app\tests\test_first_testing_command.py
.\.python312-embed\python.exe -m pytest
git diff --check
```

Do not run the full local real-image `--hash-image` command to completion. If you do a real-image smoke, keep it explicitly bounded to startup/progress/interruption behavior and report how it was bounded/stopped. Do not include private evidence paths, internal filenames, metadata values, or file content in the handoff.

## Documentation

Update active docs and ticket indexes so:

- S4.5-IMP12 is marked `Review` when complete;
- S4.5-IMP11 remains in its existing review state and is not accepted by this ticket;
- S5-T02 remains paused/deferred unless the user explicitly resumes Stage 5;
- the manual guide explains progress, safe stopping, interrupted status, and the difference between `not_run`, `interrupted`, failed, and completed digest states.

## Final Handoff

Report:

- changed files;
- progress display/artifact behavior;
- interrupted hash status behavior and whether a digest is omitted on interruption;
- `--json-only` behavior while progress is active;
- focused and full test results;
- any bounded real-image smoke result and how it was stopped;
- confirmation that S4.5-IMP11 was not marked done, S5-T02 was not started, and no full 1 TB hash completion, search/timeline, recursive crawl, broad enumeration, UI/reporting, dependency install, private evidence commit, commit, or push was done.
