# 2026-07-22 - S4.5-IMP09 Nested Directory Navigation Prompt

```text
Implement ticket S4.5-IMP09: Nested Directory Navigation Demo.

S4.5-IMP08 has been reviewed as code-acceptable for the explicit image-hash capability. Stage 5 remains blocked. Do not start S5-T01 or any S5-T02+ work.

Read:
- tickets/stage-4.5/S4.5-IMP09-nested-directory-navigation.md
- app/backend/api/first_testing.py
- app/backend/forensic_core/filesystem_adapter.py
- app/backend/forensic_core/volume_discovery.py
- app/docs/manual-testing/stage-4.5-command-line-testing-guide.md
- tickets/stage-4.5/README.md
- tickets/stage-5/README.md
- review.md
- functionality.md

Implement explicit nested directory navigation for the real E01-backed filesystem path. The goal is to prove the demo can list actual filesystem entries inside a directory, not just partitions or root counts.

Required command options:
- `--list-directory-path`
- `--demo-list-first-directory`

Optional:
- `--list-directory-id`, only if the current parser-backed file-id shape can be resolved safely. If it cannot be implemented cleanly, leave id selection deferred with honest docs/status instead of blocking the real path/demo requirement.

Required:
- list direct children of a requested directory below root through pytsk3/EWF parser APIs;
- `--demo-list-first-directory` may probe readable allocated root-directory candidates until it finds a nonempty nested listing, but must not recursively crawl the volume;
- write `outputs/directory-listing.json`, `outputs/directory-listing.csv`, and `outputs/navigation-readiness.json`;
- include status, selector mode, requested/resolved path or id, entry count, file count, directory count, parser backing, provenance, warnings, read-only assertion, and source-modified assertion;
- keep no-selection root listing behavior intact;
- no recursive crawl, no search/timeline index, no content extraction, no selected-file auto-export/preview/hash/signature;
- shared handoffs report counts/status/type counts only, not real internal names/paths/content;
- manifest, command summary, artifact inventory, docs, and tests are updated.

Run:
- `.\.python312-embed\python.exe -m pytest app\tests\test_filesystem_adapter.py app\tests\test_directory_listing.py app\tests\test_first_testing_command.py`
- `.\.python312-embed\python.exe -m pytest`

Run this real-image smoke against ` Test Image/`:

`.\.python312-embed\python.exe -m app.backend.api.first_testing --evidence-dir ".\ Test Image" --first-segment "C16242-1-RL1-E003.E01" --case ".test-artifacts\first-testing\nested-navigation-real-image" --output ".test-artifacts\first-testing\nested-navigation-real-image\outputs" --demo-list-first-directory --redact-paths`

The smoke must list one actual nested directory and produce a nonzero nested entry count. It should report at least one regular file if available; if the first readable directory only contains directories/special entries, try another bounded root-directory candidate before claiming the smoke passed. Do not quote names or internal paths in the handoff.

When complete, mark S4.5-IMP09 as Review, keep S4.5-IMP10 pending, and stop.
```
