# S4.5-IMP10 - Demo Guide And Stage 5 Gate Refresh

Status: Draft

Stage: Stage 4.5 - first testing implementation runway extension

Owner: VS Code coding agent

Reviewer: Research/review agent

## Objective

Refresh the command-line demo guide and Stage 5 gate packet after the image-level hash, nested-directory navigation, and live command-line browser tickets are implemented and reviewed.

This is the final reconciliation ticket for the reopened Stage 4.5 runway. It should make sure the user-facing guide shows the improved demo path and that S5-T01 is not rerun until the new hash and navigation requirements are accepted.

## Preconditions

Do not start this ticket until:

- S4.5-IMP08 image-level verification hash artifact is reviewed;
- S4.5-IMP09 nested directory navigation demo and S4.5-IMP09A correction are reviewed;
- S4.5-IMP09B live command-line browser is reviewed.

## Scope Boundary

This is documentation/status/review reconciliation only.

Allowed:

- update the command-line testing guide with image-hash and nested-directory commands;
- update artifact inspection steps;
- update reviewer transcript templates;
- update Stage 4.5 and Stage 5 gate docs;
- update status docs and prompt indexes.

Not allowed:

- no parser behavior changes;
- no hashing implementation changes;
- no directory navigation/browser implementation changes;
- no Stage 5 S5-T02+ work;
- no UI/report/PDF/deleted recovery/carving/packaging.

## Required Guide Updates

Update `app/docs/manual-testing/stage-4.5-command-line-testing-guide.md` so a user can run a fuller hands-on demo:

1. basic no-selection E01 run;
2. full logical-image hash command and inspection of `image-hash.json`;
3. nested directory navigation command and inspection of `directory-listing.json` / CSV;
4. live command-line browser command with `dir`, `cd`, `cd ..`, and `exit`;
5. static HTML/file-list review;
6. proof boundaries and privacy notes.

The guide must say plainly:

- image hash is an independent computed logical-image hash, not stored EWF metadata;
- nested directory navigation and the live browser list actual filesystem entries from inside the image, but they are not a recursive crawl;
- selected-file preview/export/hash/signature still require explicit selection;
- search/timeline remains Stage 5 and still waits for S5-T01 rerun.

## Required Gate Updates

Update the Stage 5 hard gate so S5-T01 checks:

- S4.5-IMP01 through S4.5-IMP07 reviewed/done;
- S4.5-IMP08 reviewed image-level hash artifact;
- S4.5-IMP09 reviewed nested directory navigation, including S4.5-IMP09A;
- S4.5-IMP09B reviewed live command-line browser;
- S4.5-IMP10 guide/gate refresh.

After this ticket is accepted, the next practical action should be S5-T01 rerun, not S5-T02.

## Tests

Documentation-only by default.

Run:

```powershell
.\.python312-embed\python.exe -m pytest
```

If guide command examples changed materially, run the safe no-selection real-image smoke and any reviewed hash/navigation inspection commands that are practical without exposing sensitive names.

## Acceptance Criteria

- The guide includes copyable commands for the no-selection demo, image hash, and nested directory navigation.
- The guide explains what each command proves and does not prove.
- Stage 5 gate docs require S4.5-IMP08, S4.5-IMP09/S4.5-IMP09A, and S4.5-IMP09B before S5-T01 can pass.
- Stage 5 S5-T02+ remains blocked until S5-T01 rerun.
- No source behavior or new forensic capability is added in this reconciliation ticket.

## Handoff Prompt

Use `prompts/vscode-agent/2026-07-22-s4.5-imp10-demo-guide-and-stage-5-gate-refresh.md`.
