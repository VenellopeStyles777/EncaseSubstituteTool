# S4.5-IMP10 - Demo Guide And Stage 5 Gate Refresh

Status: Done

Stage: Stage 4.5 - first testing implementation runway extension

Owner: VS Code coding agent

Reviewer: Research/review agent

## Objective

Refresh the command-line demo guide and Stage 5 gate packet after the image-level hash, nested-directory navigation, and live command-line browser tickets are implemented and reviewed.

This is the final reconciliation ticket for the reopened Stage 4.5 runway. It should make sure the user-facing guide shows the improved demo path and that S5-T01 is not rerun until the new hash and navigation requirements are accepted.

This ticket does not create new forensic capability. It packages the reviewed Stage 4.5 demo into clear instructions and gate records so the user can repeat the hands-on test and the reviewer can safely rerun S5-T01 next.

## Preconditions

Do not start this ticket until:

- S4.5-IMP08 image-level verification hash artifact is reviewed;
- S4.5-IMP09 nested directory navigation demo and S4.5-IMP09A correction are reviewed;
- S4.5-IMP09B live command-line browser is reviewed.

## Scope Boundary

This is documentation/status/review reconciliation only.

Allowed:

- update the command-line testing guide with no-selection, image-hash, nested-directory, browser, selected-file-template, and cleanup commands;
- update artifact inspection steps for JSON/CSV/HTML outputs;
- update reviewer transcript templates with privacy-safe status/count fields;
- update Stage 4.5 and Stage 5 gate docs so S5-T01 can be rerun after review;
- update status docs and prompt indexes.

Not allowed:

- no parser behavior changes;
- no hashing implementation changes;
- no directory navigation/browser implementation changes;
- no Stage 5 S5-T02+ work;
- no UI/report/PDF/deleted recovery/carving/packaging.
- no real-image content excerpts, filenames, internal paths, metadata values, or screenshots in shared docs.

## Required Guide Updates

Update `app/docs/manual-testing/stage-4.5-command-line-testing-guide.md` so a user can run a fuller hands-on demo:

1. clear optional cleanup step for `.test-artifacts/first-testing/<demo-run>` so outputs are fresh;
2. basic no-selection E01 run;
3. full logical-image hash command and inspection of `image-hash.json`;
4. nested directory navigation command and inspection of `directory-listing.json`, `directory-listing.csv`, and `navigation-readiness.json`;
5. live command-line browser command with `dir`, `cd`, `cd ..`, `pwd`, `help`, and `exit`;
6. static HTML/file-list review;
7. selected-file preview/export/hash/signature template only, with explicit approval and root-entry selection required;
8. proof boundaries and privacy notes.

The guide must say plainly:

- image hash is an independent computed logical-image hash, not stored EWF metadata;
- full logical-image hashing can be long-running on the local 1 TB logical image and should not be implied complete unless the command actually finishes;
- nested directory navigation and the live browser list actual filesystem entries from inside the image, but they are not a recursive crawl;
- selected-file preview/export/hash/signature still require explicit selection;
- search/timeline remains Stage 5 and still waits for S5-T01 rerun.

The guide must include copyable PowerShell command blocks that use the repository-local portable runtime:

```powershell
.\.python312-embed\python.exe
```

The local ignored evidence example may use:

```powershell
--evidence-dir ".\ Test Image" --first-segment "C16242-1-RL1-E003.E01"
```

Shared summaries must keep this path redacted or avoid quoting private path content beyond the already ignored folder command examples.

## Required Artifact Inspection Updates

The guide should tell the user what to open or inspect after each command:

- no-selection run: `run-manifest.json`, `command-summary.txt`, `outputs\demo-readiness.json`, `outputs\root-listing.json`, `outputs\file-list.json`, `outputs\file-list.csv`, and `outputs\reports\summary.html`;
- image hash run: `outputs\image-hash.json`, with status, algorithm, digest only if completed, bytes hashed, logical media size, byte-count match, read-only assertion, and source-modified assertion;
- nested navigation run: `outputs\navigation-readiness.json`, `outputs\directory-listing.json`, and `outputs\directory-listing.csv`, with parser backing, entry counts, file/directory/other counts, selected depth, file-visible status, and warnings;
- live browser run: terminal output only by default, with no transcript or artifacts unless a future ticket adds that behavior;
- selected-file template: selected-file readiness, preview, analysis, export, and export manifest only when an explicit approved parser-backed selection is supplied.

The guide should provide PowerShell-friendly inspection snippets using `Get-Content ... | ConvertFrom-Json` where helpful, but it should not require the user to understand Python internals.

## Required Gate Updates

Update the Stage 5 hard gate so S5-T01 checks:

- S4.5-IMP01 through S4.5-IMP07 reviewed/done;
- S4.5-IMP08 reviewed image-level hash artifact;
- S4.5-IMP09 reviewed nested directory navigation, including S4.5-IMP09A;
- S4.5-IMP09B reviewed live command-line browser;
- S4.5-IMP10 guide/gate refresh.

After this ticket is accepted, the next practical action should be S5-T01 rerun, not S5-T02.

Status docs must say:

- S4.5-IMP01 through S4.5-IMP10 are complete only after this ticket is reviewed and accepted;
- S4.5-IMP10 is `Review` at implementation handoff, not `Done`;
- S5-T01 is the next recommended ticket after S4.5-IMP10 review;
- S5-T02 through S5-T16 stay `Draft` and blocked until S5-T01 passes.

## Tests

Documentation-only by default.

Run:

```powershell
.\.python312-embed\python.exe -m pytest
```

Also run, if practical without exposing sensitive names:

```powershell
.\.python312-embed\python.exe -m app.backend.api.first_testing --evidence-dir ".\ Test Image" --first-segment "C16242-1-RL1-E003.E01" --case ".test-artifacts\first-testing\s4-5-imp10-guide-no-selection" --output ".test-artifacts\first-testing\s4-5-imp10-guide-no-selection\outputs" --demo-list-first-directory --redact-paths --json-only
```

Do not run the full `--hash-image` command to completion unless it is explicitly practical; the logical image is large. If it is not run, say so plainly and leave the command documented as a user/reviewer long-running option.

## Acceptance Criteria

- The guide includes copyable commands for the no-selection demo, image hash, and nested directory navigation.
- The guide includes the live browser command and the minimal in-browser command sequence.
- The guide includes artifact inspection steps and a reviewer transcript template that reports only statuses/counts.
- The guide explains what each command proves and does not prove.
- Stage 5 gate docs require S4.5-IMP08, S4.5-IMP09/S4.5-IMP09A, and S4.5-IMP09B before S5-T01 can pass.
- Stage 5 S5-T02+ remains blocked until S5-T01 rerun.
- No source behavior or new forensic capability is added in this reconciliation ticket.
- The handoff recommends S5-T01 rerun as the next practical ticket after review acceptance.

## Implementation Notes

- Updated `app/docs/manual-testing/stage-4.5-command-line-testing-guide.md` with hands-on PowerShell flows for optional fresh-output cleanup, no-selection first-testing, optional full logical-image hashing, nested directory navigation, live browser use, file-list/static HTML inspection, and selected-file testing only after explicit approved selection.
- Added `app/docs/manual-testing/stage-4.5-demo-showcase.md` as a short privacy-safe presentation walkthrough for the same documented command-line workflow.
- Updated active Stage 4.5 and Stage 5 status/gate docs so S5-T01 must check S4.5-IMP01 through S4.5-IMP10 before S5-T02 or later search/timeline implementation can start.
- Kept S4.5-IMP10 documentation/status-only. No app source behavior, parser behavior, hashing behavior, navigation/browser behavior, dependencies, evidence handling behavior, UI/report/PDF, deleted recovery, carving, packaging, transcript persistence, Stage 5 search/timeline, commit, or push was added.
- Full logical-image hashing remains documented as explicit and potentially long-running for the local 1 TB logical image. The smoke for this ticket intentionally left `outputs\image-hash.json` at `not_run`.

## Verification

- `.\.python312-embed\python.exe -m pytest`: 207 passed in 49.26s.
- Privacy-safe real-image no-selection/navigation smoke exited 0 with `ok_with_unsupported_sections`, 53 segments, metadata `metadata_available`, verification `not_supported`, EWF stream `ok`, logical media size 1,024,209,543,168 bytes, 5 volumes, filesystem `ok`, root listing `real_parser_backed` with 11 entries, file-list JSON/CSV `ok` with 11 entries, nested directory listing `ok` / `real_parser_backed` with 19 entries, files=19, directories=0, other=0, static HTML created, image hash `not_run`, selected-file operations `not_run`, `source_modified: false`, and `read_only_asserted: true`.

## Reviewer Acceptance

Accepted on 2026-07-23. No blocking findings were found.

Reviewer verification:

- Full portable-runtime run: `.\.python312-embed\python.exe -m pytest`: 207 passed in 49.87s.
- Reviewer real-image no-selection/navigation smoke exited 0 with `ok_with_unsupported_sections`, metadata `metadata_available`, verification `not_supported`, EWF stream `ok`, 5 volumes, filesystem `ok`, root listing `real_parser_backed` with 11 entries, file-list JSON/CSV `ok` with 11 entries, nested listing `real_parser_backed` with 19 entries, files=19, directories=0, other=0, static HTML created, image hash `not_run`, selected-file operations `not_run`, `source_modified: false`, and `read_only_asserted: true`.

This completes the extended Stage 4.5 runway. The next practical action is S5-T01 rerun. S5-T02 through S5-T16 remain blocked/draft until S5-T01 passes after this acceptance.

## Implementation Handoff Requirements

When finished, report back to the research/review agent with:

- changed files;
- confirmation that no app source behavior changed;
- focused/full test command results;
- whether a real-image no-selection/navigation smoke was rerun;
- whether full image hashing was run or intentionally not run;
- confirmation that no private filenames, internal paths, metadata values, file content, screenshots, real evidence files, or generated sensitive outputs were committed;
- confirmation that S5-T02 through S5-T16 remain blocked/draft.

## Handoff Prompt

Use `prompts/vscode-agent/2026-07-22-s4.5-imp10-demo-guide-and-stage-5-gate-refresh.md`.
